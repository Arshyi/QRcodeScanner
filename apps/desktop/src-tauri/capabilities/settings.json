{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-ipc",
  "description": "Allows QRForge's local settings and result-chooser webviews to invoke narrow typed commands.",
  "windows": ["settings", "results"],
  "permissions": []
}
