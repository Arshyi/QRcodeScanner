# Lifecycle benchmark results

`lifecycle.json` is produced by the release binary's `--lifecycle` mode. File output is used because the Windows GUI subsystem does not expose stdout for a release Tauri executable.

